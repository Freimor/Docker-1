docker run -d --name lair -p 8080:80 --link spawning-pool:mysql wordpress
# --link обмен информацией между контейнерами по внутренней сети docker,невидимой для хоста 
# :mysql локальное имя, используемое внутри управляющего контейнера для обращения к контейнеру адресату